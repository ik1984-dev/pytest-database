# Импорт необходимых компонентов SQLAlchemy
from sqlalchemy import Column, Integer, String, Text  # Типы колонок для БД
from app.database import Base  # Базовый класс для всех моделей


# 🎯 МОДЕЛЬ ПОЛЬЗОВАТЕЛЯ ДЛЯ БАЗЫ ДАННЫХ
class User(Base):
    """
    SQLAlchemy модель для таблицы пользователей
    Определяет структуру таблицы в базе данных
    """

    # 📊 НАЗВАНИЕ ТАБЛИЦЫ В БАЗЕ ДАННЫХ
    __tablename__ = "users"  # Имя таблицы в PostgreSQL

    # 🔑 ПЕРВИЧНЫЙ КЛЮЧ - УНИКАЛЬНЫЙ ИДЕНТИФИКАТОР
    id = Column(
        Integer,  # Тип данных: целое число
        primary_key=True,  # Это первичный ключ (уникальный идентификатор)
        index=True  # Создает индекс для быстрого поиска по ID
    )

    # 👤 ИМЯ ПОЛЬЗОВАТЕЛЯ
    name = Column(
        String(50),  # Тип данных: строка макс. 50 символов
        nullable=False  # Обязательное поле (не может быть NULL)
    )

    # 📧 EMAIL ПОЛЬЗОВАТЕЛЯ
    email = Column(
        String(100),  # Тип данных: строка макс. 100 символов
        unique=True,  # Уникальное значение (не может быть дубликатов)
        index=True,  # Создает индекс для быстрого поиска по email
        nullable=False  # Обязательное поле (не может быть NULL)
    )

    # 📝 БИОГРАФИЯ ПОЛЬЗОВАТЕЛЯ
    bio = Column(
        Text  # Тип данных: текст (неограниченной длины)
        # nullable по умолчанию True - поле может быть пустым
    )